pipeline {
    agent any
    stages { 
        stage('checkout SCM') {
            steps {
              sh "git clone https://github.com/srinivas-0/sample-pipeline.git"
            }
        }
    }
}
